// Two leaderboard modes (toggle between them):
// 1. P&L leaderboard — models ranked by today's profit/loss percentage
// 2. Level leaderboard — models ranked by current level and XP
// Both show as horizontal scrollable bar with medal icons for top 3
// Clicking a model in either leaderboard scrolls to its card in the grid
// Level leaderboard shows rank badge color per model
